# Image Processor - Вариант 5

## Описание проекта

Это приложение для обработки изображений, реализующее методы из варианта 5 учебного задания. Программа включает два основных направления:

1. **Построение и эквализация гистограммы изображения + линейное контрастирование**
2. **Реализация нелинейных фильтров, основанных на порядковых статистиках**

Приложение предоставляет графический интерфейс для удобной работы с различными методами обработки изображений.

## Функциональные возможности

### 1. Методы работы с гистограммами и контрастом
- **Линейное контрастирование** с настраиваемыми параметрами
- **Эквализация гистограммы** (3 варианта):
  - Для полутоновых изображений
  - Для каждого канала RGB отдельно
  - Только для канала яркости в HSV
- Визуализация гистограмм исходного и обработанного изображений

### 2. Нелинейные фильтры на порядковых статистиках
- **Медианный фильтр** - эффективное удаление импульсного шума
- **Минимальный фильтр** (эрозия) - выделение темных областей
- **Максимальный фильтр** (дилатация) - выделение светлых областей
- **Фильтр срединной точки** - усреднение минимума и максимума
- **Альфа-усредненный фильтр** - с отбрасыванием крайних значений

## Требования к системе

### Программные требования
- Операционная система: Windows 7/8/10/11, Linux, macOS
- Python 3.8 или выше
- Библиотеки Python (указаны в requirements.txt)

### Аппаратные требования
- Процессор: 1 ГГц или выше
- ОЗУ: 2 ГБ или больше
- Свободное место на диске: 100 МБ

## Установка и запуск

### Способ 1: Установка из исходного кода

1. Клонируйте репозиторий или скачайте архив с проектом:
```bash
git clone <URL_репозитория>
cd image-processor
```

3. Запустите приложение:
```bash
python3 main.py
```

### Способ 2: Использование готового исполняемого файла (Windows)

1. Скачайте архив с приложением
2. Распакуйте архив в любую папку
3. Запустите файл `ImageProcessor.exe`

## Структура проекта

```
image-processor/
├── main.py                    # Основной файл приложения
├── requirements.txt           # Список зависимостей
├── README.md                  # Документация
├── test_images/               # Тестовые изображения (создаются автоматически)
│   ├── low_contrast.jpg      # Низкоконтрастное изображение
│   ├── noisy.jpg             # Зашумленное изображение
│   ├── blurred.jpg           # Размытое изображение
│   ├── lines_and_points.jpg  # Изображение с точками и линиями
│   └── gradient.jpg          # Изображение с перепадами яркости
└── screenshots/              # Скриншоты интерфейса
```

## Использование приложения

### Интерфейс пользователя

Приложение имеет три основные области:
1. **Панель управления** (слева) - выбор методов и параметров
2. **Область отображения** (центр) - исходное и обработанное изображения
3. **Информационная панель** (справа) - описание методов

### Пошаговая инструкция

1. **Загрузка изображения**
   - Нажмите кнопку "Загрузить изображение"
   - Выберите файл в формате JPG, PNG, BMP или TIFF
   - Или используйте готовые тестовые изображения из списка

2. **Выбор метода обработки**
   - Выберите метод из выпадающего списка
   - Настройте параметры (размер ядра, значения контраста и т.д.)

3. **Применение метода**
   - Нажмите кнопку "Применить метод"
   - Результат появится в правой части экрана

4. **Анализ результатов**
   - Нажмите "Показать гистограммы" для сравнения распределения яркости
   - Используйте "Сохранить результат" для сохранения обработанного изображения

### Тестовые изображения

Приложение автоматически создает 5 типов тестовых изображений:

| Изображение | Описание | Для тестирования |
|-------------|----------|------------------|
| Низкоконтрастное | Малая разница между светлыми и темными областями | Линейное контрастирование, эквализация гистограммы |
| Зашумленное | Случайные точки различной яркости | Медианный фильтр, альфа-усредненный фильтр |
| Размытое | Плавные переходы без четких границ | Фильтры повышения резкости |
| Точки и линии | Четкие геометрические элементы | Обнаружение границ, морфологические операции |
| Градиент | Плавное изменение яркости от темного к светлому | Адаптивные методы, пороговая обработка |

## Теоретическая основа

### Линейное контрастирование
Преобразование яркости пикселей по формуле:
```
g(x,y) = α * f(x,y) + β
```
где α - коэффициент контраста, β - коэффициент яркости.

### Эквализация гистограммы
Равномерное распределение значений яркости по всему диапазону:
```
s_k = T(r_k) = (L-1) * Σ(p_r(j)), j=0..k
```
где L - количество уровней яркости, p_r - вероятность появления уровня яркости r_k.

### Нелинейные фильтры на порядковых статистиках
1. **Медианный фильтр**: значение пикселя = медиана значений в окрестности
2. **Минимальный/максимальный фильтр**: значение = min/max значений в окрестности
3. **Фильтр срединной точки**: значение = (min + max) / 2
4. **Альфа-усредненный фильтр**: среднее значение после отбрасывания d крайних значений

## Требования и критерии оценки

Приложение соответствует всем требованиям задания:

### ✅ Реализованные требования
- **Графический интерфейс**: интуитивно понятный интерфейс на Tkinter
- **База тестовых изображений**: 5 типов изображений создаются автоматически
- **Методы из варианта 5**: все указанные методы реализованы полностью
- **Документация**: подробные комментарии в коде и README файл

### 📊 Критерии оценки
1. **Корректность работы методов (50 баллов)**: 
   - Все методы работают корректно
   - Обработка цветных и полутоновых изображений
   - Настройка параметров в реальном времени

2. **Подборка изображений (20 баллов)**:
   - 5 категорий тестовых изображений
   - Изображения создаются автоматически
   - Каждое изображение демонстрирует конкретные особенности

3. **Пояснения (30 баллов)**:
   - Подробная документация
   - Теоретическое обоснование методов
   - Примеры использования

## Создание исполняемого файла (EXE)

Для создания standalone версии приложения:

1. Установите PyInstaller:
```bash
pip install pyinstaller
```

2. Создайте EXE файл:
```bash
pyinstaller --onefile --windowed --name ImageProcessor main.py
```

3. EXE файл будет находиться в папке `dist/`

### Проблема: Изображение не отображается
**Решение**: Проверьте:
1. Поддерживается ли формат файла
2. Не поврежден ли файл
3. Достаточно ли памяти

### Проблема: Медленная работа фильтров
**Решение**: Для больших изображений используйте меньший размер ядра или дождитесь завершения обработки.

## Дальнейшее развитие проекта

Возможные улучшения:
1. Добавление новых методов обработки
2. Пакетная обработка изображений
3. Экспорт/импорт настроек
4. Поддержка GPU-ускорения
5. Создание плагинов для расширения функциональности

## Технические детали

### Используемые библиотеки
- **OpenCV** - обработка изображений
- **PIL/Pillow** - работа с изображениями
- **Tkinter** - графический интерфейс
- **Matplotlib** - построение гистограмм
- **NumPy** - числовые операции

### Архитектура приложения
Приложение построено по модульному принципу:
- `ImageProcessorApp` - основной класс приложения
- Методы обработки разделены по функциональности
- Интерфейс отделен от логики обработки
